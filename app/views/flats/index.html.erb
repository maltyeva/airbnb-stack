<h1>Flats</h1>
<ul>
  <% @flats.each do |flat| %>
    <li class="flat">
      <%#= flat.name %>
      <%= link_to flat['name'], flat_path(flat["id"]) %> -  (<%= flat['price'] %><%= flat['priceCurrency'] %>)
      <p><%#= link_to "Learn More", flat_path(flat["id"]) %></p>
    </li>
  <% end %>
</ul>